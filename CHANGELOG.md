# Changelog

All notable changes to `new-project-starter` will be tracked here.

The repository uses Release Please to open release PRs and publish semantic version tags such as `v0.1.1`, `v0.2.0`, and `v1.0.0` from conventional commits.
