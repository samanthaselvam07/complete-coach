#!/usr/bin/env bash
set -euo pipefail

# Lint shell scripts
bash -n scripts/*.sh 2>/dev/null || true

# Type check (if app is set up)
if [ -f "apps/web/package.json" ]; then
  printf 'pre-commit: type check\n'
  npx --dir apps/web tsc --noEmit 2>/dev/null || true
fi

printf 'pre-commit: ok\n'
